apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "theapp.fullname" . }}-test-connection"
  labels:
    {{- include "theapp.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "theapp.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
